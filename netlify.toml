# Configuración de Build
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Regla de redirección para la API
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200